{
    "scripts": {
        "build": "json-server-auth db.json -r routes.json --watch"
    },
    "dependencies": {
        "json-server": "^0.17.3",
        "json-server-auth": "^2.1.0"
    }
}
